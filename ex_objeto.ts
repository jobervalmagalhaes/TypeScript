// Definindo um objeto "pessoa" com propriedades e valores
const pessoa: { nome: string, idade: number, cidade: string } = {
    nome: "Alice",
    idade: 23,
    cidade: "São Paulo",
};

// Acessando e exibindo informações do objeto
console.log(`Nome: ${pessoa.nome}, Idade: ${pessoa.idade}, Cidade: ${pessoa.cidade}`);