import { somar, subtrair } from './math';

const s1 = 5; const s2 = 3;
const resultadoSoma = somar(s1, s2);
console.log(`${s1} + ${s2} = ${resultadoSoma}`);

const d1 = 10; const d2 = 4; 
const resultadoSubtracao = subtrair(d1, d2);
console.log(`${d1} - ${d2} = ${resultadoSubtracao}`);